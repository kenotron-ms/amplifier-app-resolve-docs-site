---
title: "Event Vocabulary"
description: "Every event your resolver emits and every platform event you can consume — the full three-tier taxonomy."
---

import { Aside, Card } from '@astrojs/starlight/components';
import Mermaid from '../../../../components/Mermaid.astro';

Events are the primary observability surface in Resolve. Every instance produces an
append-only `events.jsonl` stream. This page covers both what the **platform emits**
(Tier 1 — stable contract) and what **your resolver should emit** (the `resolver:*`
cross-resolver vocabulary).

---

## Event envelope

Every event — regardless of tier — shares a single JSON schema:

```json
{
  "schema_version": 1,
  "timestamp": "2026-03-08T10:31:27.123456+00:00",
  "instance_id": "a1b2c3d4e5f6",
  "event_type": "resolver:phase",
  "phase": "implementing",
  "data": { "name": "implementing", "previous_phase": "planning" }
}
```

| Field | Type | Description |
|-------|------|-------------|
| `schema_version` | `integer` | Schema version — currently `1`. Check for forward compatibility |
| `timestamp` | `string` | ISO 8601 UTC |
| `instance_id` | `string` | Instance this event belongs to |
| `event_type` | `string` | Tier-qualified event name |
| `phase` | `string\|null` | Resolver phase at time of emission. `null` for Tier 1 events before resolver starts |
| `data` | `object` | Event-type-specific payload. Always a dict, may be `{}` |

---

## Three-tier model

<Mermaid code={`
flowchart LR
    subgraph T1 ["Tier 1 — resolve.* · Stable Contract"]
        direction TB
        t1a["resolve.container.creating"]
        t1b["resolve.worker.ready"]
        t1c["resolve.instance.status_changed"]
        t1d["resolve.input.requested"]
    end

    subgraph T2 ["Tier 2 — resolver.* · Cross-Resolver Vocab"]
        direction TB
        t2a["resolver:created"]
        t2b["resolver:phase"]
        t2c["resolver:artifact"]
        t2d["resolver:completed"]
    end

    subgraph T3 ["Tier 3 — {resolver_name}.* · Per-Resolver"]
        direction TB
        t3a["{name}.plan_created"]
        t3b["{name}.phase_gate"]
    end

    Platform["Platform Host"] -->|emits| T1
    SDK["SDK / Resolver Base"] -->|emits| T2
    Custom["Your Resolver Code"] -->|emits| T3

    style T1 fill:#1e3a5f,stroke:#3b82f6,color:#93c5fd
    style T2 fill:#1a3d2b,stroke:#22c55e,color:#86efac
    style T3 fill:#3d1a1a,stroke:#ef4444,color:#fca5a5
`} caption="Three-tier event taxonomy — Tier 1 is the stable contract external tools depend on" />

**Tier 1** events fire regardless of which resolver handles the instance. Any
dashboard, CI system, or agent can rely on these always being present.

**Tier 2** events are the `resolver:*` vocabulary — a small agreed set that every
resolver SHOULD emit to enable cross-resolver views (progress bars, artifact lists,
phase indicators).

**Tier 3** events are entirely the resolver's business. The platform never interprets
them. Consumers SHOULD handle unknown event types gracefully (log and skip).

---

## Tier 1: Platform events (stable contract)

### Container & setup

| Event Type | When | Data Fields |
|-----------|------|-------------|
| `resolve.container.creating` | Container provisioning begins | `resolver_name`, `instance_id` |
| `resolve.setup.starting` | First setup command begins | `step`, `total`, `description`, `resolver_name`, `instance_id` |
| `resolve.setup.progress` | Each subsequent setup step | `step`, `total`, `description`, `resolver_name`, `instance_id`, `exit_code`?, `success`? |
| `resolve.setup.completed` | All setup commands succeeded | `resolver_name`, `instance_id`, `job_id` |
| `resolve.setup.failed` | Setup phase failed | `cause`, `error`, `error_class`, `stage`, `instance_id` |

**`resolve.setup.failed` cause values:**

| `cause` | When |
|---------|------|
| `setup_commands` | A `container.setup_commands` entry returned non-zero |
| `repo_clone` | A workspace repository failed to clone |
| `container_init` | Container creation or image pull failed |
| `manifest_parse` | Resolver manifest is invalid |
| `gitea_sidecar` | Gitea workspace sidecar failed to start |

### Worker lifecycle

| Event Type | When | Data Fields |
|-----------|------|-------------|
| `resolve.worker.launched` | Resolver process started | `resolver_name`, `instance_id`, `job_id` |
| `resolve.worker.ready` | First state update received from resolver | `instance_id` |
| `resolve.worker.failed` | Resolver process disconnected or crashed | `cause`, `reason`?, `error`?, `instance_id` |

**`resolve.worker.failed` cause values:**

| `cause` | When |
|---------|------|
| `resolver_disconnected` | Resolver process exited unexpectedly |
| `start_failed` | `ResolverProcess.start()` raised |
| `initialize_failed` | `ResolverProcess.initialize()` raised |
| `container_died` | Poll detected container exit |

<Aside type="caution" title="Dual emission on failure">
A single failure can emit **two** `resolve.worker.failed` events within ~1 second
with different `cause` values — one from the worker side, one from the monitor poll.
**Consumers MUST deduplicate by `instance_id` + time window (5 seconds)**, not by
event count.
</Aside>

### Instance status

| Event Type | When | Data Fields |
|-----------|------|-------------|
| `resolve.instance.status_changed` | Any lifecycle status transition | `from`, `to`, `reason`? |

Status values in `from`/`to`:

```
created → starting → running → awaiting_input → running (loop)
                   → running → paused → running (after resume)
                   → running → completed
                   → running → failed
                   (any)    → cancelled
```

### Interaction

| Event Type | When | Data Fields |
|-----------|------|-------------|
| `resolve.input.requested` | Resolver opened an input request | `request_id`, `prompt`, `schema`, `status` |
| `resolve.resolver.session_dispatched` | LLM session created via `dispatch_session()` | `session_name` |
| `resolve.resolver.session_completed` | LLM session returned | `session_name`, `success`, `duration_seconds` |

---

## Tier 2: Resolver event vocabulary (`resolver:*`)

Eight events that every resolver SHOULD emit. The platform uses these for
cross-resolver views — phase indicators, progress bars, artifact listings,
completion detection.

### `resolver:created` — announce identity (emit once at start)

```json
{
  "event_type": "resolver:created",
  "data": {
    "resolver_name": "my-resolver",
    "resolver_version": "1.0.0",
    "instance_id": "a1b2c3d4e5f6",
    "params": { "spec": "Add GET /ping", "repo": "myorg/myrepo" },
    "capabilities_granted": ["gitea"]
  }
}
```

### `resolver:phase` — phase transition (emit at each phase boundary)

```json
{
  "event_type": "resolver:phase",
  "data": {
    "name": "implementing",
    "previous_phase": "planning",
    "attrs": { "plan_steps": 5 }
  }
}
```

Phase names are free-form strings — pick short, stable values. The platform displays
them as-is.

### `resolver:progress` — quantitative progress (optional)

```json
{
  "event_type": "resolver:progress",
  "data": {
    "milestone": "verifying 3 of 5 endpoints",
    "fraction": 0.6,
    "label": "Running acceptance tests against the deployed service"
  }
}
```

Use `"fraction": null` for indeterminate progress.

### `resolver:artifact` — typed output (emit per deliverable)

```json
{
  "event_type": "resolver:artifact",
  "data": {
    "kind": "pr",
    "value": "https://github.com/myorg/myrepo/pull/42",
    "metadata": { "branch": "resolve/add-ping-endpoint", "commits": 3 }
  }
}
```

**`kind` values:**

| Kind | `value` | When to use |
|------|---------|-------------|
| `pr` | PR URL | A pull request was created |
| `branch` | Branch name | A branch was pushed (no PR yet) |
| `commit` | Commit SHA | A commit was created |
| `file` | File path | A file artifact was produced |
| `summary` | Text | A human-readable summary |
| `verdict` | `"pass"` / `"fail"` / `"partial"` | A reality check verdict |
| `error` | Error description | An error artifact |
| `other` | Anything | Unclassified artifact |

### `resolver:input_requested` — blocked on input (SDK-emitted)

Automatically emitted by the SDK when your resolver calls `request_input()`. Do not
emit this yourself.

```json
{
  "event_type": "resolver:input_requested",
  "data": {
    "request_id": "gate-001",
    "surface_id": "build-gate",
    "prompt": "Build failed after 3 attempts. How should we proceed?",
    "timeout_s": 3600
  }
}
```

### `resolver:input_responded` — input satisfied (SDK-emitted)

Automatically emitted by the SDK when an input request is satisfied or times out.

```json
{
  "event_type": "resolver:input_responded",
  "data": {
    "request_id": "gate-001",
    "outcome": "submitted",
    "duration_s": 142.3
  }
}
```

`outcome` values: `submitted` | `timeout` | `cancelled`

### `resolver:health` — stress signals (emit when under stress)

```json
{
  "event_type": "resolver:health",
  "data": {
    "signals": {
      "context_used_frac": 0.82,
      "errors_60s": 3,
      "blocked_on": "awaiting reality-check"
    }
  }
}
```

**Recommended signal keys** (not required, but interpreted by cross-resolver views):

| Key | Type | Meaning |
|-----|------|---------|
| `context_used_frac` | `float` 0–1 | LLM context window fill fraction |
| `errors_60s` | `int` | Recent error count (60s window) |
| `llm_p95_ms` | `int` | Recent 95th-percentile LLM latency |
| `worker_count` | `int` | Active worker processes (if resolver spawns workers) |
| `blocked_on` | `string` | Free-form description of what the resolver is waiting on |

### `resolver:completed` — terminal event (emit exactly once before exit)

<Aside type="danger" title="This event is required">
This is the most important event in the vocabulary. The platform distinguishes
**clean exit from crash** by the presence or absence of `resolver:completed`. If
your process exits without emitting it, the platform marks the instance as crashed.

Emit it on **every** exit path, including exception handlers.
</Aside>

```json
{
  "event_type": "resolver:completed",
  "data": {
    "outcome": "success",
    "summary": "PR created: https://github.com/myorg/myrepo/pull/42",
    "duration_s": 847.2,
    "artifact_count": 3
  }
}
```

**On failure:**
```json
{
  "event_type": "resolver:completed",
  "data": {
    "outcome": "failed",
    "summary": "Build gate failed after max retries",
    "duration_s": 423.1,
    "artifact_count": 0,
    "error": {
      "code": "build_gate_exhausted",
      "detail": "5 consecutive build failures with no progress"
    }
  }
}
```

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| `outcome` | enum | ✓ | `success` \| `partial_success` \| `failed` \| `cancelled` |
| `summary` | string | ✓ | One-line description |
| `duration_s` | float | ✓ | Wall-clock time from `resolver:created` |
| `artifact_count` | int | ✓ | Number of `resolver:artifact` events emitted |
| `error` | object | on failure | `code` (string) + `detail` (string) |

---

## Reality check events (Tier 1, broker-emitted)

When a resolver invokes the reality-check capability, these events ride the same
SSE stream:

| Event Type | When | Key Payload Fields |
|-----------|------|-------------------|
| `resolve.reality_check.submitted` | Broker accepted the request | `session_id`, `instance_id` |
| `resolve.reality_check.runner_started` | Runner container booted | `session_id`, `runner_image` |
| `resolve.reality_check.recipe_step` | Each recipe step transition | `session_id`, `step_name` |
| `resolve.reality_check.dtu_provisioned` | DTU sandbox ready | `session_id`, `dtu_id`, `access_urls` |
| `resolve.reality_check.completed` | Verdict returned | `session_id`, `verdict`, `failure_mode` |
| `resolve.reality_check.failed` | Infrastructure failure | `session_id`, `failure_mode`, `error` |
| `resolve.reality_check.cancelled` | Cancelled | `session_id`, `reason` |

---

## Consuming events

### Stream via SSE (recommended)

```bash
curl -N "$RESOLVE_URL/api/instances/a1b2c3d4e5f6/events" \
  -H "Authorization: Bearer $TOKEN"
```

```python
import json, requests

def stream_events(instance_id: str, base_url: str, token: str):
    headers = {"Authorization": f"Bearer {token}"}
    with requests.get(f"{base_url}/api/instances/{instance_id}/events",
                      headers=headers, stream=True) as resp:
        for line in resp.iter_lines():
            if line:
                text = line.decode()
                if text.startswith("data: "):
                    event = json.loads(text[6:])
                    if event.get("type") == "done":
                        break
                    yield event
```

### Extract status transitions

```python
def get_status_timeline(events):
    return [
        {"from": e["data"]["from"], "to": e["data"]["to"], "ts": e["timestamp"]}
        for e in events
        if e.get("event_type") == "resolve.instance.status_changed"
    ]
```

### Design principles

1. **Files are the API.** `events.jsonl` on disk IS the contract. SSE is a convenience layer.
2. **Never-crash policy.** Failed event writes are swallowed silently — gaps are possible.
3. **Tier 1 is stable.** New Tier 1 events may be added; existing ones won't change without a `schema_version` bump.
4. **Flush-on-write.** Every event is flushed immediately — `tail -f events.jsonl` works.
5. **Atomic state.** `state.json` written via `os.replace()` — you never see a partial write.
